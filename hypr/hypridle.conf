general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

# TTE screensaver after 5 minutes
# listener {
#     timeout = 300
#     on-timeout = bash -c '[ -f /tmp/tte-start ] && exit 0; date +%s > /tmp/tte-start && kitty --title tte-screensaver -e ~/dotfiles/scripts/tte-screensaver.sh'
#     on-resume = bash -c 'start=$(cat /tmp/tte-start 2>/dev/null || echo 0); now=$(date +%s); [ $((now - start)) -gt 2 ] && pkill -f tte-screensaver && rm -f /tmp/tte-start'
# }
